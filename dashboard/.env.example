# Base URL of the proxy's control API this dashboard watches.
# Local dev default (used automatically if unset): http://localhost:8000
# For the hosted dashboard, set this to the deployed proxy's public URL as
# a Vercel environment variable at build time.
VITE_PROXY_URL=http://localhost:8000
