# No secrets are required to run the Phase 1 proxy: it forwards whatever
# Authorization header the calling SDK sends, it does not hold its own
# provider key. Budget and hosted-demo config land in Phase 4.
